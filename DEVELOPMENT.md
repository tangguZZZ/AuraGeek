# 开发边界

- 固件唯一入口 `platformio.ini`，库在 `lib_deps` 声明，由 PlatformIO 安装至 `.pio/libdeps/<env>`。不改第三方库文件，不复制 SDK 到工程。项目专用 `platforms_dir`、`packages_dir`、`cache_dir` 将 AuraGeek 的 pioarduino 平台、Arduino 3.3.11 和工具包与用户级官方 PlatformIO 隔离；`.pio-core` 为可再生成缓存且不提交版本库。VS Code 注入的 `PLATFORMIO_CORE_DIR` 优先于 ini，因此不能只依赖项目级 `core_dir`。
- PC 模拟器入口 `simulator/CMakeLists.txt`，工具在 `D:/LVGL-Dev`；`simulator/build` 是可再生构建目录。
- UI 唯一实现 `src/ui/AuraUi.*`，PC/ESP32 共用，禁止另建不能移植的 HTML/XML 假预览。
- `UiService` 持有 LVGL 生命周期；应用主循环独占所有 LVGL 调用。网络后台任务只交付快照。
- `DisplayDriver` 保护实物基线：rotation=1、BGR、INVERSION_OFF、RGB565 `pushColors(..., true)`、HSPI 20 MHz。
- GIF 为内置压缩资源；LVGL 384 KiB 内存池在 PSRAM。上电必须检测 PSRAM。
- 没有传感器数据用占位；没有股票原始缓存时用明确标注 REF CACHE 的真实离线参考快照；没有 USB 音频用静止环与未连接状态。AI 页面纯黑，仅显示 GIF，无文字。
- 新资源从 `Doc/UI素材` 取，PNG 用官方 `LVGLImage.py` 转 RGB565A8，GIF 用 RAW。输出 `src/ui/resources`。
- `SpectrumAnalyzer` 只消费连续 512 个 16 位单声道 PCM；USB 接收端必须先完成立体声合并、缓冲与采样率匹配。PC 测试信号不能进入固件。
- PC 存完整日线；固件每标的最多 12000 条完整日线存 LittleFS，文件校验长度、日期顺序和 checksum，PSRAM 用于运行数据。先在完整日线计算 MA20/MA55，再按 6M/1Y/3Y/5Y/ALL 裁切、最多 280 点抽样绘图。顶部百分比是区间价格涨跌，不是当日涨幅或含分红总收益。
- 股票原始源为东方财富 fqt=0 不复权；参考快照从用户站点导入，绝不混合两种口径。参考 QQQ 起于 2001-01-02、VOO 起于 2010-09-09，末日为 2026-09-04；ALL 指当前源可用历史，不保证上市以来无遗漏。
- 上海周二至周六 09:00 后调度，每标的每天最多 3 次尝试，成功即停止；请求前持久化预算及冷却时间。禁止重启绕过预算、切页触发请求、轮换域名重试。请求串行，标的间隔 2–2.5 秒；普通失败 60/300 秒退避，403 至少一天，429 至少一小时，并尊重数字秒数形式 Retry-After。PC 脚本另支持 HTTP-date 形式。
- 增量从最后缓存日期重叠更新末条；更早历史修订需要显式维护，不保证供应商永不修订历史。JSON 仅保留元数据，日线逐条处理并让出 CPU，避免初次历史解析触发看门狗。
- 当前 HTTPS 使用未验证证书模式；量产前必须补可信 CA / 证书验证，不能描述为已完成安全加固。任何请求间隔都不能保证免受限流。
- 网络密码只在 `include/config/Secrets.local.h`。
- ESP32 使用项目私有 pioarduino 55.03.311 / Arduino-ESP32 3.3.11 官方 USBAudioCard：原生 USB GPIO19/20、UAC1、48 kHz S16 双声道、仅播放端点。Windows 名称通常为“耳机 (TinyUSB UAC1)”。原生 USB COM12 被声卡替代是预期行为；CH343 COM9 用于烧录与日志，不承载 USB 音频。
- `arduino-audio-tools` 1.2.5 也提供 ESP32-S3 UAC2 RX，但该功能于 2026-06 才加入。当前继续使用已经通过 1 kHz/3 kHz 实机验证的 Espressif UAC1，不因工具隔离改变音频行为。
- USB 回调写入有界 PCM 缓冲，主循环取最新连续 512 点做 FFT；满时丢弃旧样本以保持显示实时性，不提供录音级无丢帧保证。需要把电脑播放器输出指定到此声卡，不会自动捕获另一块声卡的声音，也未提供模拟音频播放输出。
- 仅接屏幕时，可在 CH343 115200 baud 输入 `ui home/menu/stocks/spectrum/agent`、`ui next`、`range 0..4`、`stock status` 调试；量产可关闭 `AURAGEEK_BENCH_COMMANDS`。实物 EC11、温湿度、外接三个 WS2812 仍需接线验证。
- CLI 使用 VS Code 官方 `.platformio/penv`，不要混用旧 Python 3.10 的全局 PIO。库通过 lib_deps 管理；不得修改下载的第三方源码来规避编译错误。
- 小智语音端到端未实现；当前仅待机表情页。
- 编译、烧录、串口运行和屏幕视觉验收分别说明；不得用动画截图宣称真实音频或 AI 链路完成。
