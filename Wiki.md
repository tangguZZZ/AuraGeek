# AuraGeek 当前调试基线

更新：2026-09-15。工程：D:/My_Project/AuraGeek。

项目总览、架构与首次构建流程见 [README](README.md)；本页维护当前接线、详细操作及实机验证边界。

## 当前硬件与固件

- 主控：ESP32-S3-WROOM-1-N16R8，16MB Flash、8MB OPI PSRAM。
- 屏幕：当前2.4寸TFT＋EC11一体模块，ST7789、SPI、无触摸，横屏320×240。
- 固件：AuraGeek原应用；PlatformIO环境esp32-s3-devkitc-1，Arduino-ESP32 3.3.11，LVGL 9.5.0。
- SPI当前配置80MHz，用户反馈目前未发现异常；这不代表已完成长期稳定性或无撕裂验收。
- 背光GPIO18，5kHz/8bit PWM，当前最近启动读回亮度100%。
- AHT20已连接。下载/串口日志使用CH343 COM9、115200 baud，实际端口以枚举为准。
- WS2812只使用开发板自带的一颗灯，GPIO48；不增加外接灯，不预留外接灯接口。
- Micro SD卡及SPI读卡模块已购买、尚未到货；只固定接口定义，未接线、未启用驱动、未验收读写。
- SD卡接好前，暂不开发SD读写、资源迁移或新的存储优化，也不重新规划与SD相关的内存、缓存和文件分配；到货接线后再统一评估。现有NVS设置掉电保存、Flash内资源与行情缓存保持当前基线，不清空已保存配置。
- 当前不修改屏幕接口和接线，继续用本固件调试。小智官方服务已支持并验收唤醒应答、停顿自动发送和轮流连续聊天；回声消除、边听边说及长时间稳定性不在已验收范围。

## 当前接线

### TFT＋EC11一体模块12针

| 模块针脚 | 信号 | ESP32-S3 |
|---:|---|---|
| 1 | GND | GND |
| 2 | VCC | 3.3V |
| 3 | SCL/CLK | GPIO12 |
| 4 | SDA/MOSI | GPIO11 |
| 5 | RES | GPIO14 |
| 6 | DC | GPIO13 |
| 7 | CS | GPIO10 |
| 8 | BLK | GPIO18 |
| 9 | A | GPIO39 |
| 10 | B | GPIO40 |
| 11 | PUSH | GPIO21 |
| 12 | KEY0 | GPIO41 |

GPIO编号为开发板IO丝印，不是排针位置编号。模块与开发板共地。PUSH、KEY0低电平有效；模块输入已有上拉/滤波，固件仍使用INPUT_PULLUP。

### 其他已启用接口

| 模块 | 信号 | ESP32-S3 / 状态 |
|---|---|---|
| AHT20 | SDA / SCL | GPIO8 / GPIO9，3.3V、GND，I²C 100kHz，地址0x38 |
| 板载WS2812 | DIN | GPIO48 |
| CH343 / UART0 | ESP TX / RX | GPIO43 / GPIO44，用于下载与日志 |
| 原生USB | D- / D+ | GPIO19 / GPIO20，用于UAC音频输入 |

原生USB音频口与CH343下载口不是同一个功能接口。WS2812仅板载一颗，不接外部灯模块。

### AHT20温湿度模块（已启用）

| 模块丝印 | 接到 | 说明 |
|---|---|---|
| VCC / VIN | ESP 3.3V | 使用3.3V，避免模块上拉把I²C拉到5V |
| GND | ESP GND | 共地 |
| SDA | GPIO8 | 数据线，不与SCL接反 |
| SCL | GPIO9 | 时钟线 |

按模块丝印认线，不按排针从左到右猜顺序。当前100kHz、地址0x38、每2秒采样；模块若没有I²C上拉，需要补到3.3V的上拉电阻，不能在未核对模块电路时反复并联上拉。

### INMP441麦克风

以下沿用当前SPI阶段原有音频引脚，不改变屏幕和按键。用户已确认接好线，小智唤醒及聊天收音已跑通；不同距离和环境下的识别表现仍需专项验证。

| 模块丝印 | 接到 | 说明 |
|---|---|---|
| VDD / VCC | 3.3V | 不接5V |
| GND | ESP GND | 共地 |
| WS | GPIO4 | I²S左右声道时钟 |
| SCK / BCLK | GPIO5 | I²S位时钟 |
| SD | GPIO6 | 麦克风输出，ESP输入；不是使能脚 |
| L/R | GND | 固定左声道，不悬空 |
| CHIPEN（若引出） | 3.3V | 高电平使能；普通模块未引出时核对板载连接 |

模块供电旁需去耦；SD在非输出时为高阻，原厂建议100kΩ下拉，模块已有时不重复添加。连接依据：[INMP441原厂资料](https://invensense.tdk.com/wp-content/uploads/2015/02/INMP441.pdf)。

### MAX98357A功放与喇叭

当前独立I²S接线：BCLK7、LRC15、DIN16、SD_MODE17。**GPIO10仍是屏幕CS，不能接功放控制。** 当前调试供电按用户最新决定改为3.3V。此前小智语音、唤醒应答及设置音量已验收，但3.3V供电下的音量、稳定性和电源余量需重新确认；USB音乐听感仍按下文保留未验收边界。

| 模块丝印 | 接到 | 说明 |
|---|---|---|
| VIN / VDD | 合规3.3V电源 | 模块须支持3.3V；若取自开发板3V3引脚，须核对稳压器剩余电流及发热，勿从GPIO供电 |
| GND | 电源GND及ESP GND | 必须共地 |
| BCLK | GPIO7 | ESP输出位时钟 |
| LRC / LRCLK / WS | GPIO15 | ESP输出声道时钟 |
| DIN | GPIO16 | ESP输出音频数据 |
| SD / SD_MODE | GPIO17，经模块电平核对后连接 | 低电平关闭，高电平使能/选择声道；不是音频数据输入 |
| GAIN | 保留模块默认配置 | 不接GPIO，不随意短接VIN或GND |
| SPK+ / OUT+ | 喇叭正端 | 差分功率输出 |
| SPK- / OUT- | 喇叭负端 | 不接ESP GND或电源GND |

芯片供电范围2.5–5.5V，具体模块以其规格为准。GPIO17只允许3.3V逻辑，若模块SD存在5V上拉，须先处理电平隔离/上拉，禁止直接把5V带入ESP。喇叭为XHXDZ-3525-8Ω2W-2P1.25，规格书标注8Ω±15%、额定2W、最大输入2.5W。当前按用户要求默认数字音量0dB，不额外衰减或放大；这不等于实际输出功率已经测定，电脑音量仍可控制，出现破音应降低电脑音量。停止音频时钟前先关闭功放。外部5V供电不得与开发板USB电源无隔离并联反灌。连接依据：[MAX98357A原厂资料](https://www.analog.com/en/products/max98357a.html)。

### WS2812灯（仅板载一颗，已启用）

板载灯由开发板内部接到GPIO48，无需额外接线。代码使用kOnboardWs2812=48和RMT输出，GRB颜色顺序；当前不启用外接灯，不将其他DIN并到GPIO48。GPIO47用于下方SD片选预留，不再属于灯接口。

| 应用状态 | 当前灯效 |
|---|---|
| 待机 | 低亮度青蓝紫渐变呼吸 |
| 连接服务端 / 思考 | 紫色系呼吸 |
| LISTENING听音 | 青色呼吸 |
| SPEAKING播放应答或回复 | 绿色系波动；不是实际音量频谱 |
| 聊天错误 / 显示初始化失败 | 红色双闪 |

### Micro SD / TF卡模块（接口已预留，硬件未到、未启用）

针对当前购买的带稳压、电平转换的6针SPI模块，固定如下接口；按模块丝印接线，不按排针顺序猜测。代码定义位于include/board/BoardPins.h，此处预留不是已完成SD驱动。

| 模块丝印 | ESP32-S3连接 | 代码定义 / 方向 |
|---|---|---|
| VCC | 开发板5V电源轨 | 模块VCC供电，不是GPIO信号电压 |
| GND | ESP GND | 共地 |
| SCK / CLK | GPIO42 | kSdClock，ESP输出 |
| MOSI / DI | GPIO1 | kSdMosi，ESP→卡 |
| MISO / DO | GPIO2 | kSdMiso，卡→ESP，必须为3.3V逻辑 |
| CS / SS | GPIO47 | kSdChipSelect，ESP输出，低有效 |

- 总线预留kSdSpiHost=SPI2_HOST，经GPIO矩阵映射到上述管脚；屏幕使用SPI3_HOST，不共享显示屏的80MHz总线。后续接驱动必须显式传入这些引脚，不能用默认SPI引脚覆盖屏幕。
- 模块资料为MicroSD Card Adapter v0.9b：PDF标注VCC 4.5–5.5V，选型图标注4.5–5V，两者统一按5V供电；板载LDO为卡和电平转换芯片提供3.3V，文档说明MISO返回3.3V。不要将该5V版模块的VCC直接接3.3V，也不能让ESP的GPIO承受5V。到货后核对实物版本、丝印及电平，资料不是实物电气验收。
- 资料列出模块典型80mA、最大200mA；与功放共用5V时需核对供电余量，禁止外部5V与USB无隔离并联反灌。所有接线、初次插拔卡在断电状态进行。
- 资料覆盖Micro SD≤2GB、SDHC≤32GB及FAT16/FAT32；已购买卡的容量、格式尚未确认，不能据此保证SDXC/exFAT支持。6针接口未引出卡检测/写保护信号，不另占GPIO。
- 当前没有SD挂载、读写、热插拔或自动格式化功能。设置仍存NVS，股票缓存仍用LittleFS，语音模型和表情资源仍在板载Flash；不会因未插卡影响现有功能。
- 当前常规空闲GPIO原有1、2、38、42、47；分配SD后仅剩GPIO38可另行规划（基于本板已确认板载灯为GPIO48）。GPIO0/3/45/46为启动配置相关脚，不列作普通空闲；GPIO26–37涉及Flash/OPI PSRAM，不用于此预留。GPIO42占用后不可再同时用于外部四线JTAG。

资料：[模块用户手册](<Doc/Micro SD卡模块 TF卡读写卡器 SPI接口 带电平转换芯片/MicroSD_Card_Adapter用户文档.pdf>)。芯片约束参考[Espressif GPIO说明](https://docs.espressif.com/projects/esp-idf/en/v5.1/esp32s3/api-reference/peripherals/gpio.html)和[SPI主机说明](https://docs.espressif.com/projects/esp-idf/en/v5.1/esp32s3/api-reference/peripherals/spi_master.html)。

### 下载与原生USB（已启用）

- 下载/日志：电脑USB线插开发板CH343口；板内已连接ESP TX43→CH343 RX、ESP RX44←CH343 TX，无需另拉跳线。串口115200 baud，当前COM9。
- 音频：另一条USB数据线插ESP原生USB口，板内D-→GPIO19、D+→GPIO20；电脑播放器输出选择TinyUSB UAC1。不要使用只有充电功能的线。
- 不对现有开发板额外手接USB差分线，也不把下载口当成音频口。

所有模块接线在断电状态进行；以模块丝印而非未经确认的排针位置编号为准。已启用模块与仅预留SD接口分开列明；当前不采用外接WS2812。

## 当前页面与操作

菜单五项：HOME、STOCKS、SPECTRUM、AGENT、SETTINGS。

| 页面 | 当前功能 |
|---|---|
| HOME | NTP时间/日期、秒环、天气、AHT20温湿度、天气24小时温度趋势、Wi-Fi状态 |
| STOCKS | 自选日线（默认QQQ/VOO）、半年/1年/3年/5年/全部、两条可配置均线、中文数据状态 |
| SPECTRUM | 音乐律动倒影和无文字呼吸环形，双击打开样式轮盘 |
| AGENT | 小智官方聊天、你好小陈唤醒及语音应答、自动断句、半双工连续问答、状态/情绪表情 |
| SETTINGS | 背光、喇叭播放音量、切页模式、默认频谱、只读设备信息、WEB CONFIG网页配置入口；确认后NVS保存，频谱增益固定200% |

- 首页转动EC11进入菜单；菜单旋转选项，单击进入。
- 股票页旋转切换五档范围，单击按网页保存的顺序切换自选标的，右下角显示当前位置/总数；只有一个标的时保持不变。
- 总菜单采用无惯性拖尾的短缓动，停稳后不继续移动；保留一格一项，不增加旋转加速。快速往返时按最终累计格数确定选项，实际编码器误计数与动画滞后须分别检查。
- 频谱页双击打开轮盘，旋转选择，单击确认；再次双击或KEY0短按取消。轮盘关闭时单击触发视觉脉冲。
- 频谱或Agent页旋转返回菜单。
- EC11双击在其他页面执行返回；EC11长按1.2秒回首页。
- KEY0短按返回或取消，长按0.8秒回首页。
- 设置页旋转选项，单击编辑，再次单击确认；返回取消未确认编辑。

EC11旋转解码与按钮采样分离。A/B两相均使用变化中断，并一次读取同一GPIO寄存器中的两相信号；完成一个合法四相周期才报一格，触点回弹和未完成的往返会抵消，两位同时跳变则丢弃本次未完成计数并重新同步。不会根据单个A相上升沿猜方向，也不靠放大时间死区压低灵敏度。按钮仍约2ms独立采样、20ms去抖，双击窗口300ms。原按键组合、股票五档和频谱双击轮盘已验收；新的旋转解码已烧录，用户在正反向逐格测试后确认恢复正常；此前持续单向旋转偶发反跳的问题按本次实物反馈验收。极端高速、长线干扰等场景不在本次验收范围内。

### 设置保存

板内设置与网页配置分开：背光、喇叭音量、切页模式、默认频谱只在板内调整，网页不重复提供。WEB CONFIG入口需单击进入确认，再次单击才重启进入配置模式；返回可取消。

### 网页配置

当前工程已实现设备热点配置模式；手机网页可修改首页配色，保存并退出后生效，断电重新上电仍恢复最后保存的配色，此完整链路已由用户实机确认。自动弹窗及AP/STA网络隔离仍待分别验收，不能以模拟器通过代替实机验证。

- 入口：设置页的WEB CONFIG；或非软件复位开机时按住KEY0约3秒；正常运行持续无法联网约60秒也会进入配置模式。
- 连接：手机连接屏幕显示的`AuraGeek-Setup-xxxx`热点；前期测试按用户要求采用固定密码`88888888`，仍使用WPA2。此密码仅用于开发调试，不作为出厂安全方案。当前默认地址`http://192.168.4.1`，无法自动弹出时手动输入。
- 配网：扫描或手填2.4GHz Wi-Fi，提交后测试连接约20秒；连接成功才保存新凭据。密码错误保留热点及旧凭据，可再次输入。目标网络与热点网段冲突时当前拒绝保存，并显示原因；尚未实现自动换网段。
- 再次进入配置后，可在“网络接入”点击“使用已保存的 Wi-Fi”联网测试 AI，无需重新填写密码。密码不回传网页；开发固件内置网络不计入此入口。连接测试必须完成一次新的断开、连接和获取IP过程，不将旧连接误判为新密码有效；失败保留热点和上次已保存凭据。
- 配置模式暂停聊天、麦克风唤醒及USB音频，不同时运行正常联网工作任务。完成后网页点击退出，或释放入场按键后再按KEY0，重启恢复设备。当前没有热点自动关闭计时。
- 网页参数保存到独立NVS命名空间，退出配置并重启后生效；不会覆盖板内音量、背光等设置，也不会删除原有官方服务绑定。

| 网页栏目 | 当前支持内容 | 边界 |
|---|---|---|
| 首页个性化 | 背景、文字、强调色；经典/薄荷/浅色/紫色预设 | 只作用首页，不改变现有布局；文字对比度至少4.5，强调色至少3 |
| 设备与位置 | 设备名称、首页英文城市标签、经纬度、UTC偏移 | 城市标签最多20字节；时差−12至+14小时，15分钟步进 |
| 股票自选 | 1–6个标的及顺序；短均线2–120、长均线3–250个交易日；两条均线分别显示/隐藏 | 短周期须小于长周期；默认QQQ/VOO、MA20/MA55；保存退出后生效；不开放行情地址或刷新频率 |
| AI交互 | 唤醒开关、唤醒应答开关、连续聊天开关 | 唤醒词仍固定“你好小陈”；未提供任意文字改唤醒词 |
| 聊天节奏 | 断句静音600–2400ms，未说话等待5–20秒，单句上限10–30秒 | 未说话等待不能大于单句上限；断句30ms、无声等待100ms、录音上限1秒步进 |
| AI服务 | 保留现有官方服务，或配置兼容当前小智WebSocket协议的WSS地址、Token及协议版本1–3 | 不是任意OpenAI接口；证书校验不关闭；自定义服务仍待端到端联调 |
| 配置管理 | 导入/导出schema=1的网页JSON，最大4KiB；未列出的字段保持不变 | 导出不含Wi-Fi密码或Token；未知字段/类型错误/越界配置拒绝保存 |

输入规则在网页和设备端分别校验，失败时保留原配置，不截断字符串后强行保存：

| 输入项 | 允许内容与限制 |
|---|---|
| 设备名称 | 最多32个UTF-8字节；中英文、数字、空格、`- _ . ( ) · （ ）`，不能首尾空格或只有符号；常用中文通常每字3字节，网页显示字节计数 |
| 首页城市标签 | 最多20字节；英文/拼音、数字、空格、`- _ . ( )`；当前屏幕字体不支持中文城市字形 |
| Wi-Fi名称 | 1～32字节有效UTF-8，可包含中文、英文、常见符号和表情；不得含控制字符、隐藏方向字符；空格不自动删除，须与真实SSID一致 |
| Wi-Fi密码 | 开放网络留空；否则8～63位可打印ASCII字符，或64位十六进制；支持英文、数字、符号、空格，不支持中文，不自动修剪 |
| WSS地址 | 最多256字节；英文域名或完整IPv4、可选1～65535端口；要求`wss://`，不接受账号、查询参数、片段、原始中文或空格；中文路径需URL编码 |
| 服务Token | 最多512位可打印ASCII，不得含中文、控制字符或只有空格；留空保持原值，清除必须勾选明确清除；不回显已保存值 |
| 数字与颜色 | 数字须为正确类型、范围和适用步进；经纬度允许有效小数，禁止用文字、NaN或无穷值代替数字；颜色须为`#RRGGBB`并满足对比度 |

名称字段不接收HTML标签或任意符号串；Wi-Fi名称中的合法特殊字符仍按文本显示，不作为HTML执行。名称或城市格式不合规时，保存被阻止，输入框旁显示错误，底部提示未保存，并自动定位到出错项；修正后可重新保存。中文输入过程不自动删除或改写文字。格式校验不判断一个合法名称是否“有意义”，例如合法范围内的随机英文字母仍可作为名称。
旧固件已保存、但不符合新版符号规则的可打印名称仍可读取，避免升级时连带丢失原配色；下一次保存或导入按新版规则校验，需先修正名称。不会在升级时自动改写名称。

AI服务页提供“测试当前填写的连接（不保存）”：先在网络接入连接家庭Wi-Fi，再验证证书校验的WSS及当前固件支持的Opus/单声道/60ms语音hello。测试不打开麦克风、不发送聊天内容、不保存服务参数或更改官方绑定；首次测试自建服务会建立独立、稳定的本机连接身份。测试结果区分网络时间失败、连接失败、授权拒绝、协议不兼容和内存不足。握手通过不代表语音聊天、模型或角色已经验收；此新增测试功能的真实服务端联调仍待完成。

角色介绍、模型、音色、服务端记忆等仍在[官方控制台](https://xiaozhi.me/console/agents)管理。网页JSON导入并不是官方智能体配置导入接口，不会通过本地保存冒充已修改云端角色。任意模型接口、AEC/AGC/NS、静态IP及网页OTA当前均未开放。

#### 恢复产品出厂默认

入口：网页「配置管理」→「恢复产品出厂默认」。必须输入 **恢复出厂** 并勾选确认，再点击「确认恢复并重启」；取消不会改变设置。JSON 导入只修改网页参数，恢复出厂则是独立的整机用户配置操作。未保存的表单即使有错误，也可取消编辑后选择恢复。

- 恢复音量、亮度、转场、频谱样式、首页配色与位置、股票自选及均线、聊天节奏与唤醒开关；清除用户保存的 Wi-Fi、自建语音服务地址、Token 和自建客户端身份。
- **保留官方 AI 绑定、官方客户端身份及连接凭据**；不修改云端角色、音色、模型或记忆。保留固件、表情、语音模型；不操作 SD 卡。
- 保留行情缓存、请求预算、冷却时间与 403 暂停记录。恢复出厂不能用来绕过股票接口调用保护，也不是存储介质的安全擦除功能。
- 默认亮度/音量为 100%，首页经典黑金，自选 QQQ/VOO、MA20/MA55，唤醒及连续聊天开启，断句/等待/录音上限为 900ms/8秒/20秒。平时升级不改变已保存的 MA21；只有用户执行恢复时才回到默认均线。
- 恢复后重启进入 `AuraGeek-Setup-FE30` 热点，开发密码 `88888888`，打开 `http://192.168.4.1` 重新配网。此后没有有效用户 Wi-Fi 时继续进入配置模式，不回退到固件内置开发网络。
- 恢复意图先写入并校验，重启后按指定键清理；中途失败保留恢复标志并停止正常服务，下次重启重试，不整片擦除 NVS。网络测试或 AI 测试期间禁止恢复；请求须通过会话及配置版本校验。提交结果不明确时网页停止重复操作，需重新连接检查。

验证边界：已编译烧录，写入校验通过；正常启动与配置热点启动已通过串口检查。用户已确认在真实手机网页完整执行「确认恢复并重启」；随后串口读到默认黑金配色、默认聊天参数，板内设置使用默认值，与恢复操作一致。2026-09-15 用户进一步实机确认：重新配网后无需再次激活，可用「你好小陈」唤醒并正常聊天。因此「恢复本机用户配置 → 重新配网 → 保留官方 AI 绑定 → 唤醒并聊天」链路已验收。恢复清理、绑定与限流保留、各存储操作失败后的重试已通过隔离存储测试；确认框、取消、错误输入与跨页返回已通过本地桌面及手机尺寸浏览器检查。恢复过程中断电后的续恢复仍待实机专项验收，本次正常恢复链路通过不替代断电故障注入验证。

#### 输入错误与页面切换

详细错误留在对应输入框旁；切换到其他页面时仅显示「某页面有待修正项，输入已保留」及「返回修改」，不会把原字段长错误固定在其他页顶部。返回后自动定位、聚焦出错项；输入修改后移除旧提示，保存时再次校验。断网、保存失败或结果不确定等全局错误不会因切页被隐藏。当前跨页行为已做本地浏览器验证，真实手机上的新交互仍待验收。

自建服务使用独立保存的客户端UUID，不要求先激活官方服务，也不会覆盖官方客户端标识或绑定。标识首次生成后写入并回读校验；已有标识无效时停止连接，不静默更换身份。协议版本1为原始Opus，版本2/3分别附加16/4字节头；头长度和声明载荷长度不符时拒绝解码。当前自建身份与协议头逻辑已做本地测试，外部自建服务聊天仍待联调。

HTTP与DNS仅绑定AP地址，并校验连接地址；修改请求校验Host、Origin和随机会话Token。网页显示外部文本使用安全文本节点，不执行用户HTML；请求头最多3KiB、JSON正文最多4KiB。安全限制已实现，实际AP/STA网络隔离验证仍待完成。配网模式日志记录内部空闲内存、历史最低值、最大连续块和PSRAM余量，不输出密码或服务Token。

HTTP响应发送使用非阻塞写入，每次最多1024字节，响应头和正文共用约3秒发送预算；超时或对端断开则关闭连接，避免慢客户端让发送无限重试。这仍是有时间上限的同步响应处理，不是完整异步服务器。分段写入、持续背压、共享超时及计时回绕已做本地测试；真实手机弱网与TCP背压仍待专项验收。

启动保护：HTTP必须实际进入监听状态后才宣布配置服务就绪；HTTP启动失败时关闭HTTP、DNS和配置热点，返回现有正常启动流程，不修改已保存配置。AP或DNS启动失败同样清理已启动的配置热点。已部署，正常启动已通过串口检查；监听失败结果、回调释放及成功启动已通过本地替身测试。真实设备的端口绑定失败与资源不足场景尚未注入验证，不代表AP/STA隔离已验收。

网页保存使用读取配置时的版本号；其他页面已修改配置时拒绝覆盖，点击“重新读取”确认丢弃本页未保存修改后再编辑。JSON导出仍是schema=1配置文件，不包含会话版本。Wi-Fi扫描期间按钮保持禁用，失败后可重试；空数值、低对比度配色及不合理聊天时序在网页端提示，并由设备再次校验。

多页面配置：状态轮询发现另一网页已保存时，提前提示版本冲突，保留当前表单和 JSON 草稿，暂停保存/导入，不等点击保存后才提醒。设备重启或配置会话变化时，旧页面同时暂停网络测试、AI测试及退出等设备操作；「重新读取」成功后同步新会话与配置版本，再恢复操作。重新读取会替换表单中未保存的修改，因此有表单草稿时先确认。已经出现的“保存结果未知/已保存但回读失败”提示不会被普通冲突提示掩盖。已编译烧录并通过正常启动、配置模式启动检查；本地双浏览器页面及手机尺寸的冲突、重启、草稿保留和重新读取检查通过，真实电脑与手机同时连接开发板的联合操作仍待验收。

网页首次读取保护：未成功读取设备配置前，个性化、股票和AI参数及保存/导入按钮不可编辑；Wi-Fi输入独立保留。读取失败后可重试，成功后开放编辑。早于后续用户操作发出的旧配置/状态响应不能覆盖新页面状态；退出配置后表单保持锁定，需重新进入设备热点配置页才能继续操作。

网页保存结果保护：保存请求超时、连接中断、设备返回服务端错误或缺少有效保存确认时，显示“保存结果尚未确认”，保留输入并禁用再次保存/导入；不自动重发。若设备已确认写入但随后读取失败，则明确显示“设备已确认保存，但读取最新配置失败”。点击“重新读取”并确认替换本页未保存输入，成功取得设备当前配置后才恢复写入。普通参数校验拒绝可直接修改后重试，版本冲突须先重新读取。导入同样遵循此流程，写入后的读取失败不会清空导入文本；导入前若表单还有未保存修改，会在确认框中提示其将被重新读取替换。上述保护已部署，异常场景通过本地网页逻辑测试；手机端真实超时、断连场景仍待专项验证。

输入提示：数值越界、留空、步长不符、配色对比度不足及聊天时长冲突会显示中文字段说明，并切换到对应页面、定位错误输入；重新校验时清理已修正的旧提示。已通过本地真实浏览器的桌面及手机宽度检查，当前开发板已包含此项优化。用户已在真实手机确认输入“测试<script>”后可点击保存并出现名称错误提示；其他数值限制及跨页错误定位的真实手机操作仍待分别验收。

手机导航：收起侧栏后，其控件不再参与键盘导航；展开时遮罩拦截底层操作，点击遮罩或按 Escape 可关闭并返回菜单按钮。选择页面后关闭侧栏且保留未保存输入；手机横屏或窗口较矮时可独立滚动侧栏，访问末尾的配置管理。展开状态切回电脑宽度后自动解除底层操作锁定。已通过本地浏览器的电脑、手机竖屏及横屏检查、网页逻辑回归和固件编译，已烧录并完成启动及配置模式串口检查；实际手机连接设备后的触摸及软键盘体验仍待验收。

手机保存操作：窄屏（不超过800px）或低高度窗口（不超过500px）使用随正文滚动的操作栏，滑到当前页面底部即可操作，不再固定到配网浏览器底边。“保存网页配置”独占第一行按钮区，下方为“重新读取”和“完成并退出配置”；电脑正常高度窗口保留固定底栏。此修复针对手机配网窗口遮住换行后的保存按钮问题，已通过390×640、320×360、740×390及1280×900本地浏览器检查并烧录，用户已确认真实手机能完整看到并点击保存按钮，且非法名称出现错误提示。更新固件后需重新打开网页，不能继续操作仍停留在旧内容的窗口。

导入先调用设备端只读校验，列出字段的修改前后值，确认后才写入；取消不会保存。Token仅显示替换/清除状态，不显示内容；文件与现有配置相同时不重复写入。预览期间若其他页面已保存，最终写入仍会因版本冲突被拒绝。

关闭手机网页不主动关闭热点；如果设备已回到首页，需从设置的WEB CONFIG重新进入。扫描完成或失败由设备主循环处理，不依赖手机继续轮询；未取走的结果30秒后释放，避免关闭网页后一直阻止下一次配网。串口检查可用tools/portal_serial.py，打开CH343前关闭DTR/RTS，不主动发送复位操作。

### 板内参数范围

| 项目 | 实际对应功能和范围 | 每格变化 | 默认值 |
|---|---|---|---|
| DISPLAY | 屏幕背光PWM，10–100%；下限保留可见亮度 | 1% | 100% |
| AUDIO | MAX98357A喇叭播放总音量，0–100%；0为数字静音 | 5% | 100% |
| MOTION | 页面切换CLEAR直切 / BLEND交叉淡化；不改变所有控件自身动画 | 切换一档，到端停止 | CLEAR |
| SPECTRUM | 进入频谱页使用REFLECT倒影 / RING环形；页内轮盘仍可临时选样式 | 切换一档，到端停止 | REFLECT |
| SYSTEM | 型号、分辨率、LVGL版本、内存，只读，不写入设置 | 无 | INFO |

单击进入编辑，旋转实时预览；再次单击确认，出现SAVED后保存完成。返回取消本次编辑并恢复进入编辑前的值；KEY0回首页或语音唤醒跳离设置页，同样取消尚未确认的编辑。调节时不反复写Flash，无实际变化不写入。越界旋转停在上下限，不循环跳到另一端。亮度和音量的数字使用48px字体、百分号25px，同字体家族和基线，数字与符号整体水平居中。

确认变更才写NVS的aura-ui/settings，并读回核对；保存失败显示SAVE FAILED、恢复上次确认的配置。启动校验版本、长度、数值上下限，非法配置使用默认值。当前格式为v2：旧v1配置保留亮度、动效和频谱样式，旧频谱增益不当成喇叭音量，迁移时音量采用100%。掉电恢复最后成功保存的配置，编辑中的预览值不会保存；四项配置的软件重启读回已验证，用户已确认正常调节及实际掉电保存。

喇叭音量对小智语音、USB音乐、提示音和本地录音回放统一生效，不调整麦克风录音/VAD灵敏度。使用平方音量曲线：25%约-24dB、50%约-12dB、75%约-5dB、100%=0dB（原始幅度，不额外放大），切换有短渐变以减小爆音。USB电脑端的音量/静音仍会叠加生效。百分比不是瓦数或声压值；MAX98357A当前调试为3.3V供电、喇叭8Ω2W，100%仅限制数字增益不超过1倍，不代表已验证功率安全；更换供电后先低音量试音，出现失真、破音或异常发热应降低音量并检查电源。

频谱显示增益固定200%（2.0倍），不在设置中调节、不单独保存；显示能量限幅到0–1，不会超出画布。该值不随喇叭音量改变，也不改变录音增益。

## 当前显示实现

- TFT_eSPI只负责面板初始化：rotation=1、BGR、INVERSION_OFF；随后释放总线，由esp_lcd独占SPI3、mode0、80MHz异步DMA。
- LVGL使用RGB565_SWAPPED、PARTIAL，内部SRAM两个320×40×2字节缓冲，共50KiB。
- DMA完成后才归还绘制缓冲；LVGL对象操作由应用主循环独占，后台任务只交付数据。
- LVGL内存池为1MiB PSRAM，显示影子缓存为150KiB PSRAM；局部变化裁剪后发送。
- 当前没有TE接线，不能承诺严格无撕裂。默认CLEAR，BLEND会主动叠加新旧页面。
- 显示批次、软件定时周期不等于面板帧率。PC和ESP共享UI源码，但模拟器不能证明实屏性能。

## 当前数据与音频

### 网络、天气与传感器

正常联网优先使用网页配网验证成功后保存在NVS的Wi-Fi凭据；尚未保存时回退到开发基线include/config/Secrets.local.h中的网络列表，不提交真实密码。联网后NTP按网页保存的UTC偏移显示，默认UTC+8；天气来自Open-Meteo，查询位置使用网页保存的经纬度。天气24H曲线采用服务返回的真实小时数据，没有有效数据时显示占位。首页右上天气文字与温度保持单行、固定右对齐，按内容宽度向左展开并同步调整图标位置；用户已实机确认当前配色下文字、温度同一行，图标与文字不重叠。其他配色与长天气名称组合已通过模拟器布局检查，不等同于逐项实机验收。

AHT20后台每2秒采样，未发现设备时自动重试；无效时不生成假温湿度。当前启动日志偶发一次I²C ESP_ERR_INVALID_STATE，之后可以成功采样，该提示尚未修复。

天气与股票HTTPS使用系统CA证书包校验证书和主机名，等待有效UTC时间，不提供跳过校验的回退。仅允许固定数据来源，禁用自动重定向；响应正文分别限制为32768和900000字节，放入PSRAM，拒绝超限、截断、压缩编码或含NUL的正文。读取设有超时检查，错误不绕过股票调用预算。天气经此路径已取得有效数据；股票本轮验证的是缓存读取，新的线上响应和异常网络场景仍须在限频内验证，不代表整个网络功能已完成安全验收。

### 股票

股票页显示日线收盘价，不是实时行情。默认QQQ、VOO，可在网页“股票自选”中管理1–6个标的。主源使用东方财富日K、不复权；失败时保留明确标注的缓存，参考快照不与主源口径混合。

网页填写东方财富标的编号而不是网址：`0.`/`1.`后接六位代码，`116.`后接五位代码，`105.`/`106.`/`107.`后接美股代码（最多10个字符，大写字母、数字，允许内部单个点或连字符）。编号不能重复，不接收空格、中文、查询参数或未知市场前缀；市场前缀必须与来源一致，不能按名称猜测。空槽保存时跳过，至少保留一个。前后端均校验，非法修改不覆盖旧配置。编号格式正确不保证接口有数据；新增市场和新标的仍需按限频单独联调。

右上角明确标注所选时间范围的“区间涨跌”，按该范围首末日线收盘价计算，并非当日涨跌幅或含分红总回报。货币显示USD/CNY/HKD；日期轴右端是所展示的最后一个日线日期。界面优先中文提示，保留股票代码、货币和MA通用标识；底部区分参考快照、缓存、更新失败、限流和接口暂停。无数据不显示上一标的价格或假曲线。单点/横盘曲线有纵轴留白，小数价格不统一取整；长代码和价格按宽度适配，均线颜色与图例对应。

数据使用边界：接口可访问、调用频率低，不等于获得商用或再分发授权。产品交付给客户前应确认行情数据许可和可用接口配额，参阅[东方财富法律声明](https://about.eastmoney.com/home/disclaimer)。不通过代理轮换、伪装身份或反复更换请求参数规避服务端限制。

完整日线存LittleFS，运行数据在PSRAM。按原始日线计算均线后抽样显示。范围切换和页面重绘不发起行情请求。切换范围时保留已有图表，新范围的曲线、选中状态和区间涨跌一起更新，不清空为“暂无日线数据”，也不等待250ms常规状态更新周期；标的确实无数据时仍显示空状态。

响应须为有效JSON，只读取唯一的`data.klines`且核对唯一的`data.code`，不采用其他层级同名字段。日线日期须为1990–2099年的真实日历日期且顺序严格递增，收盘价须为0.000001–1000000000之间的有限正十进制数；不接收额外列、重复日期或乱序。当天/未来数据不发布，空响应或没有符合日期范围的行不标为更新成功。先在临时副本解析，整份响应通过后才替换缓存，后半段异常也不能发布前半段。解析使用逐行扫描，避免将全部日线字符串放入JSON去重池。此校验已通过离线异常样例和长历史测试；新线上返回仍受既有调用预算限制，未用强制刷新验收。

更新时段为上海周二至周六09:00后，与用户设置的首页时区分开；只接收本次更新日期之前的日线。每标的每天最多3次，成功后停止当日尝试；全部自选共享每日最多12次、至少60秒间隔的本机保守预算（并非官方配额）。请求前保存预算，记录失败则不请求；更换自选顺序或重启不清零全局预算。403暂停来源且保存暂停状态，429至少等待1小时，服务端Retry-After要求更久时遵守更长等待；不开放清零预算或强制刷新入口。网页只读显示最近预算记录和冷却/暂停状态，查看状态不会请求行情。

新股票配置、中文布局和调用保护已编译烧录；本地逻辑、模拟器和实机串口五档范围检查通过，设备可继续读取既有QQQ/VOO日线缓存。用户此前已通过手机将短均线修改为21，确认屏幕显示MA21且断电重新上电后仍保留，网页股票参数保存、生效与掉电恢复链路已验收。普通编译或烧录不重置已保存的均线；用户已主动执行恢复出厂，此操作按设计恢复默认MA20/MA55，不能继续把此前MA21作为当前设置。恢复后的均线实物显示、新增/排序多个自选标的、股票中文实物可读性及新增市场行情仍需分别验收，不能用单项均线设置验证代替。

### USB音频频谱

电脑将播放器输出选择为“耳机（TinyUSB UAC1）”，经原生USB向ESP输入48kHz、S16LE、双声道PCM。CH343不承载音频，固件也不会自动捕获电脑其他声卡的声音。

USB回调写入有界缓冲，主循环合并/分析音频，使用512点FFT、48频段。缓冲满时丢弃旧样本并累计overwritten_samples，当前不保证无丢样。

倒影采用音乐能量驱动的视觉律动，柱子横轴不表示精确频率；环形使用频段能量。静音时能量衰减，装饰呼吸不代表检测到声音。

USB到MAX98357A播放：48kHz立体声取均值后双声道同值输出，驱动单只喇叭。播放音量跟随设置页AUDIO，默认100%=0dB，重启恢复最后确认保存的值。保留电脑下发的USB音量/静音控制，并输出UAC HOST诊断日志；固件无自动增益控制。仅启动、欠载恢复和主动调音量时做短渐变，连续播放保持固定增益。USB回调只写PSRAM中4096样本有界队列，独立音频任务送I²S；频谱分析保持原通路。预缓冲2048样本约43ms，欠载时短淡出并重新预缓冲，溢出丢弃新样本；分别累计欠载/丢样，不承诺无丢样。上电功放静音，有USB数据才播放；USB输入停止300ms后关闭功放；持续USB流中的全零PCM不再自动切换功放使能。录音/回放测试期间暂停接收USB播放数据，完成后恢复；audio stop关闭USB播放，audio usb重新开启。用户已确认出声，仍反馈杂音及忽大忽小，当前听感未验收。

### 小智当前状态

网站角色介绍、音色、语言模型等属于服务端配置，保存后结束旧对话并重新发起会话验证，无需重新烧录。设备本地唤醒词、音频处理、屏幕表情及硬件驱动属于固件/资源配置，需要更新设备。网页勾选扩展能力不代表AuraGeek已实现对应设备工具；当前没有实现MCP设备工具和独立网络音乐播放器。MCP接入地址包含凭据，不应公开或写入Wiki。

用户已确认手动小智回复清楚、连贯，首页语音唤醒后会自动切到小陈页面，以及连续模式第二轮能自动接上、安静后能回到待机。当前固件支持一次唤醒后的轮流连续聊天：每次回复后自动继续听音，不必重复唤醒。实机串口已记录同一会话至少三轮听音、静音自动提交、识别和回复播放完成，并自动进入第四轮，随后确认设备回到空闲且唤醒引擎仍就绪。基本交互流程已验收；环境声误触发、表情实物观感、不同距离和长时间稳定性仍待验收。USB电脑音乐杂音、回声消除及语音打断不在已验收范围。

支持INMP441 16kHz单声道、3秒本地录音与MAX98357A回放测试，回放跟随设置页AUDIO音量；本地测试录音保存在PSRAM，不写Flash、不上传。待机唤醒在本地使用麦克风，不上传待机音频；正式LISTENING阶段将语音发送到服务端。单一音频任务管理麦克风DMA的申请和释放，播放回复时不同时录音。独立3秒录音回放及采样峰值、RMS、削波数仍待单独验收。

服务端为小智官方xiaozhi.me。ai provision在独立任务请求官方设备配置接口，校验HTTPS证书，要求Wi-Fi及NTP已就绪；设备标识使用ESP32自身Wi-Fi MAC，客户端UUID及会话配置保存在aura-ai，绑定码通过串口显示，服务凭据不打印。当前账号绑定及会话配置获取已成功，手动单回合Opus/WebSocket语音链路已实机验证：握手、编解码自检、语音识别、回复音频接收与播放完成，用户确认听到回复。实际协商为上行16kHz、下行24kHz、协议版本1；音质、识别准确率、连续多轮、断网恢复与取消操作仍待专项验收。不下载、不安装官方接口返回的固件，不覆盖AuraGeek自定义工程。

- 首页或其他页面空闲时说“你好小陈”，联网情况下自动进入Agent页，连接成功后向官方服务发送listen/detect唤醒通知，请求小陈先语音应答；音色与措辞由网站角色配置和服务端决定，不是本地固定录音。等应答结束并显示LISTENING后再说正文，连续聊天后续各轮不重复唤醒应答。也可进入Agent页单击EC11开始连接，手动入口直接进入听音、不请求唤醒应答。
- 唤醒应答已实机验证：本地识别、应答播放完成后开始听音、正文静音提交、回复结束继续下一轮。用户已确认能听到应答、正常接上聊天且应答音量合适；等待时间仍受网络及服务端影响。该功能不需要额外勾选网页开关，沿用已保存的音色和角色。
- 唤醒应答沿用设置页AUDIO音量（0%静音），播放时不开启麦克风；音频播放排空、功放静音、等待350ms尾音并丢弃120ms麦克风启动数据后，才开始正文听音。等待应答期间可单击/返回取消；断网或响应超时显示错误，不把未完成应答当成已就绪。当前不缓存连接/应答期间用户抢先说出的正文，不上传原始唤醒词录音，因此未实现官方声纹校验链路。实现参考[官方唤醒流程](https://github.com/78/xiaozhi-esp32/blob/main/main/application.cc)和[唤醒消息格式](https://github.com/78/xiaozhi-esp32/blob/main/main/protocols/protocol.cc)。
- 听音中累计检测到至少240ms语音后，连续安静900ms自动结束并提交；约8秒没有有效语音则回待机，单次听音最多20秒。听音时单击仍可立即提交。实际阈值效果取决于环境和语音检测，不是固定计时必然准确断句。
- 一次唤醒后复用同一条WebSocket会话进行连续问答。回复播放结束，关闭功放、等待350ms尾音消退，再重新初始化麦克风并显示LISTENING，用户直接说下一句。约8秒无有效语音则关闭本轮会话、回Agent待机，约1.5秒后恢复本地唤醒。也可按KEY0或返回退出。未实现播放回复时同时听音或语音打断。
- 连接/等待/播放期间单击可取消；返回、双击返回或旋转离开Agent同样取消。取消网络连接时可能等待当前有限超时操作退出，并非立即中断TLS调用。
- Agent保留原待机动画，另选倾听、思考、开心、生气、难过、爱心6个原始GIF。21类服务端情绪归并到这些表情，未提供情绪时用默认表情；不是逐字口型同步。切换采用70ms淡出、零透明度处替换、110ms淡入，同一播放器复用，不并行解码两张GIF；快速情绪变化合并到最新目标，离开页面取消过渡。模拟器切换和过渡检查通过，实物观感待确认。GPIO48灯继续跟随会话状态。
- 录音16kHz单声道、60ms Opus帧；下行支持16/24/48kHz单声道，协议支持版本1/2/3的帧头校验。当前连续问答是半双工（轮流说话），端点由本地VAD发送listen start/stop控制；未实现回声消除、MCP工具或连续全双工。
- 同一音频任务独占I²S与Opus，聊天期间不播放USB音乐，结束后恢复原USB开关状态。TLS分配优先使用PSRAM，保持证书和主机名校验；NVS凭据当前未启用加密，不记录语音转写全文。
- Opus依赖固定为codec-opus提交dc1920f17c947d4bb3ddc52e99e94a45dd343a19，按该版本单线程伪栈机制分配PSRAM，未修改第三方库。语音启动时执行静音帧编解码自检。
- 唤醒使用ESP-SR中文MultiNet7，命令为ni hao xiao chen，阈值0.35。语音模型来自Espressif esp-sr提交27da4f945f779bab2d238889924622f7988b1b1c，构建时校验文件哈希。模型与6个GIF位于0x800000资源分区，原NVS、应用分区和股票文件系统地址不变。项目适配绕过预编译库的英文G2P，直接提供中文拼音；模型初始化期间普通分配优先PSRAM，随后恢复Arduino分配策略。
- 上电至少5秒后初始化本地唤醒。USB音频播放或刚收到USB音频、正在聊天或音频测试期间不执行待机唤醒。初始化异常会保留保护标记，下一次启动退回手动模式，避免反复崩溃；屏幕提示文字不能代替ready状态。离线不能开始云端聊天。

音频命令：audio status查看状态及USB播放计数；audio tone播放0.5秒1kHz提示音（跟随当前输出档位）；audio record录3秒；audio play回放；audio stop停止当前测试并关闭USB播放；audio usb恢复USB播放。小智命令：ai provision申请/刷新配置、ai status查看状态、ai start进入Agent并开始/结束听音、ai stop取消当前会话。

音量调试命令：audio level full/low/normal/medium分别将当前AUDIO值设为100/25/35/50%，不会直接写NVS，重启仍恢复最后确认保存的值。日常持久化调节请使用设置页。settings status显示当前四项配置、实际应用的背光和喇叭音量、已保存音量及格式版本；audio status显示输出音量和目标Q15增益。频谱显示增益固定200%，与这些命令无关。

唤醒调试：ai status同时显示enabled和ready；enabled=1仅代表开关开启，ready=1才表示模型已就绪。ai wake off/on关闭或开启本地识别；ai wake retry仅供修复初始化故障后显式执行一次重试，不应在日常使用中反复发送。

## 当前构建与调试入口

固件使用PlatformIO，依赖以platformio.ini为准，不修改第三方库。项目.pio-core与.pio为构建/依赖目录，不提交版本库。

- 编译：`pio run -e esp32-s3-devkitc-1`
- 烧录：`pio run -e esp32-s3-devkitc-1 -t upload --upload-port COM9`
- 日志：CH343串口115200 baud。
- PC模拟器：`powershell -File tools/simulator.ps1 run`
- UI回归：`powershell -File tools/simulator.ps1 test`

模拟器使用D:/LVGL-Dev工具，入口simulator/CMakeLists.txt，输出simulator/build/windows-debug/aurageek_sim.exe。与ESP共用src/ui代码；PC测试数据和鼠标事件不等于实机验收。

串口每条命令以换行结束：ui home、ui menu、ui stocks、ui spectrum、ui agent、ui settings、ui next、ui double、ui back、ui rotate 1、ui rotate -1、range 0..4、stock status、settings status、display stats。display stats统计传输与绘制，不直接等于屏幕FPS。

本基线包含官方 AI 聊天、网页配置及保留官方绑定的恢复出厂功能；具体已验收项与待验收边界见各章节。Git 提交和合并必须由用户明确要求；编译、烧录、串口检查和实物验收分别确认。本机密码、运行日志、浏览器测试缓存及构建产物不纳入版本管理。
